'use strict';

{
  const prices = [180, 190, 200];

  // const updatedPrices = prices.map((price) => {
  //   return price + 20;
  // });

  const updatedPrices = prices.map(price => price + 20);
  console.log(updatedPrices);
}
/*mapメソッドは配列の各要素に何らかの処理をして別の配列を作る命令*/
