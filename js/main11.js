'use strict';

{
  // const point = [100, 180];

  // const point = {x: 100, y: 180};
  const point = {
    x: 100,
    y: 180,
  };
  console.log(point);
}
//「x:100」プロパティ（メンバー）、「x」名前（キー）、「100」値
/*オブジェクト記法*/