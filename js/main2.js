'use strict';

{
  const scores = [80, 90, 40];
  // console.log(scores[1]);
  /*インデックスを指定して再代入すると値が更新される*/

  // scores[2] = 44;
  // console.log(scores);

  // scores = 10;

  console.log(scores.length);
}
/*インデックスを指定して再代入すると値が更新される*/